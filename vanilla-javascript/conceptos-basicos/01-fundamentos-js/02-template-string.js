const nombre = 'DeadPool';

const real = 'Wade Wilson';

const personaje = nombre + ' => ' + real;

console.log( personaje );

console.log( `${ nombre } => ${ real }` );

const html = `
    <h1>
        Hola
    </h1>
    <p>
        Mundo
    </p>
`;

console.log( html );